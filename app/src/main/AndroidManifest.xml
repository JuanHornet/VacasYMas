<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".base.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VacasYMas"
        tools:targetApi="31">

        <!-- 🚀 Dashboard como pantalla inicial -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Otras activities -->
        <activity
            android:name=".ui.animales.BuscarAnimalActivity"
            android:exported="true" />

        <activity android:name=".ui.animales.DetalleAnimalActivity" />
        <activity android:name=".ui.diagnostico.DiagnosticoGestacionActivity" />
        <activity android:name=".ui.pesos.PesosActivity" />
        <activity
            android:name=".ui.reproduccion.RegistrarPartoActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.reproduccion.CriasPendientesActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.reproduccion.IdentificarCriaActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.crotales.CrotalesActivity" />


    </application>

</manifest>